# Comments

## Period at the end

Do not put a period `.` at the end of a comment. It is not a sentence, it is a code comment

## Parameters w/o description

Often you document a method, and your IDE auto generates a comment for each parameter.
If you do not have anything to say about a parameter, just remove the comment for it
